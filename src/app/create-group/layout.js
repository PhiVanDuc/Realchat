import CreateGroupLayout from "@/components/layouts/create-group/create-group-layout";

export default function Layout({ children }) {
    return (
        <CreateGroupLayout>
            {children}
        </CreateGroupLayout>
    )
}
